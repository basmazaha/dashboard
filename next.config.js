const nextConfig = {
  devIndicators: {
    appIsrStatus: false,
  },
  // In Next.js 15, you might need to allow the Replit host for HMR/WebSockets
  // to prevent "Cross origin request detected" warnings or connection issues.
};

module.exports = nextConfig;

